services:
  - type: web
    name: construction-backend
    env: python
    rootDir: detection_module
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    pythonVersion: 3.10.13
